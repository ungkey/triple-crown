# Phase 1 Plan — Auth Fixture

type: standard

## Goal
A user can sign in, view the dashboard, and log out. After logout the dashboard must no longer be accessible without signing in again.

## Verification
- `npm test`
- Browser QA of login → dashboard → logout → dashboard access.
