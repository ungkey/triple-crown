# Triple Crown v0.6 Harness Test Report

## Build environment

```text
Python: 3.13.5
Node: v22.16.0
npm: 10.9.2
Git: git version 2.47.3
```

## L0 aggregate

```text
PASS Triple Crown v0.6 L0 aggregate
```

Result: **PASS**

## L0 coverage

- v0.6 doctor mock mode;
- mock capability install/list;
- hook graph render + exact Triple Crown contract assertions;
- project capability staging;
- hard ship guard installer;
- inherited v0.5 ship authorization/session limits;
- docs-only push allowlist;
- release/deployment SHA binding;
- Canary deployment binding;
- document-release mutation boundary.

## Current host L1 readiness

The current ChatGPT artifact runtime is **not** a valid real GSD 1.10 target.

See:

```text
e2e/LIVE-DOCTOR-RESULT.json
```

Observed blockers:

```text
Node 22.16.0 < required Node 24
gsd command unavailable
gstack unavailable
Superpowers unavailable
```

Claude Code, gh and Bun are also unavailable here, so L2 cannot be executed in
this runtime.

## Status statement

```text
L0: PASS
L1: NOT EXECUTED — host prerequisites fail
L2: NOT EXECUTED — real AI/toolchain unavailable
```

This report intentionally does not call v0.6 fully compatible yet.
