# Triple Crown v0.6.1 Workflow Guide Test Report

## Result

```text
Syntax checks: PASS (29 JS/CJS/MJS files)
Structural validator: PASS
Workflow guide smoke: PASS
L0 aggregate: PASS
Mock staged E2E: PASS
```

## Structural validation

```text
PASS: triple-superpowers
PASS: triple-gstack
PASS: triple-crown-guide
PASS: v0.6.1 core structure / workflow guide / ship ownership
PASS: v0.6.1 E2E harness files
```

## Workflow Guide scenarios

```text
PASS guide-plan-review-blocker
PASS guide-execute-next
PASS guide-review-next
PASS guide-qa-gap->gsd-gap-plan
PASS guide-ship-next
PASS guide-deployment-mismatch
PASS guide-canary-complete
PASS Triple Crown workflow guide smoke
```

Covered:

- PLAN exists but exact gstack plan review marker is missing -> BLOCKED / `/plan-eng-review`
- approved plan not executed -> `/gsd-execute-phase N`
- execution complete but review missing -> `/gsd-triple-gstack-code-review N`
- QA/UAT issue -> `/gsd-plan-phase N --gaps`
- verification + security clear -> `/gsd-ship N`
- deployment SHA mismatch -> release-observe recovery
- matching deployment -> Canary
- Canary pass -> `/gsd-progress --next`

## Mock staged E2E

The mock install path now installs all three project capabilities:

```text
triple-superpowers
triple-gstack
triple-crown-guide
```

Stages:

```text
doctor: PASS
gsd-init: PASS
install-triple-superpowers: PASS
install-triple-gstack: PASS
install-triple-crown-guide: PASS
capability-list: PASS
staging: PASS
workflow-guide: PASS — current=plan next=/plan-eng-review
hook-contract: PASS
ship-guard-install: PASS
adapter-contracts: PASS
```

The workflow guide is executed from the **staged project capability path** before
the hook graph assertions, so the test is not limited to the source-tree script.

## L0 aggregate

```text
PASS Triple Crown v0.6.1 L0 aggregate
```

## Packaging robustness

Mock E2E no longer depends on the executable bit of `mock-gsd.cjs`; mock calls
run through `node mock-gsd.cjs`. This avoids ZIP extraction / Windows permission
differences affecting L0.

## Runtime limitation

As in v0.6, full compatibility still requires target-host L1/L2:

```text
L1 real GSD install/render-hooks
L2 real Claude Code + gstack + Superpowers semantics
```

The current artifact runtime is not promoted to a full compatibility PASS.
