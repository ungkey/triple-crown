# Triple Crown v0.6.3 Installer Test Report

## Summary

```text
Node/CJS syntax: PASS (30 files)
Direct installer: PASS
Bash wrapper: PASS
Local npm tarball via npx: PASS
Inherited Triple Crown L0: PASS
npm pack --dry-run: PASS
```

## Installer fixture

```text
PASS installer-install-idempotent-status-uninstall
```

Validated:
- project stable source `.triple-crown/`;
- all three project capabilities ACTIVE in the fake GSD lifecycle;
- managed CLAUDE routing block created once;
- ship guard installed;
- repeated install does not duplicate routing/hook registration;
- `status` uses the staged guide;
- uninstall removes only managed Triple Crown surfaces.

## Bash wrapper

```text
PASS bash-local-installer
```

Validated local/extracted release form:

```bash
bash install.sh --yes --no-bootstrap
```

## npm tarball / npx

```text
PASS npx-local-tarball-install
```

Validated the actual packed `.tgz` through:

```bash
npx --yes --package ./triple-crown-workflow-installer-0.6.3.tgz   triple-crown install --yes --no-bootstrap
```

This test confirms the installation is not accidentally relying on the source
repository tree outside the npm package.

## Inherited workflow contracts

```text
PASS Triple Crown v0.6.1 L0 aggregate
```

## Not claimed as tested

The artifact runtime cannot run the real current GSD bootstrap because its Node
runtime is below the GSD 1.10 `>=24` engine requirement.

Therefore these remain target-host validation:

```text
remote GSD bootstrap
remote gstack clone/setup
real GSD project capability install/consent
real Claude plugin discovery for Superpowers
published npm registry install
curl-from-hosted-GitHub install.sh
```

The local package, Bash wrapper, and npx tarball mechanics are executable and
passed; remote distribution still requires publishing the repository/package.
